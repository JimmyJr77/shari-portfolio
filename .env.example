# Groq API key for AI chat (never commit the real key!)
# Add to Vercel: Settings → Environment Variables → GROQ_API_KEY
# For local dev with `vercel dev`, create .env.local with:
# GROQ_API_KEY=your_key_here
